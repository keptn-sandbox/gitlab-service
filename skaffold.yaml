apiVersion: skaffold/v1beta13
kind: Config
build:
  artifacts:
    - image: localhost:5000/gitlab-service # Todo: Replace this with your image name
      docker:
        dockerfile: Dockerfile
        buildArgs:
          debugBuild: true
deploy:
  kubectl:
    manifests:
      - deploy/dev-service.yaml
